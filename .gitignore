# OS
.DS_Store
Thumbs.db

# Jekyll
blog/_site/
blog/.jekyll-cache/
blog/.jekyll-metadata
blog/vendor/

# Node / npm
node_modules/
npm-debug.log
dist/
*.tsbuildinfo

# Uploaded patient files (dev local storage — Sprint 4 moves this to S3)
src/backend/uploads/

# Python
__pycache__/
*.pyc

# Environment secrets
.env
.env.local
.env.*.local
DEV_CREDENTIALS.md

# Terraform (PSM2 — don't commit state or secrets)
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
.terraform.lock.hcl
tfplan*

# IDE
.vscode/settings.json
.idea/

# Superpowers subagent-driven-development scratch (ledgers, briefs, reports)
.superpowers/

# Local reference files — keep off remote
docs/references/Final Report Ammar yasser.pdf
docs/references/mindcraft refrence files/
.claude/
claude-sessions/
.agents/

# MCP config — contains GitHub token, never commit
.mcp.json

# PSM1 submission PDFs/DOCX — large binaries, tracked locally only
// docs/psm1-submission/

# Large visual asset folders — keep off remote
docs/clinic_imgs/
docs/Modern_Hospital_Landing_Visuals/
docs/new_landingpage/
docs/psm1-submission/MohamedMakhlouf_PSM1_FinalReport_MD_format/
